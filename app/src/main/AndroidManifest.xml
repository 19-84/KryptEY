<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

  <uses-permission android:name="android.permission.VIBRATE" />

  <!--
    Package visibility for the one query this app makes.

    From targetSdk 30 the platform filters queryIntentActivities to packages this app can already
    see, and it can see almost none. SettingsActivity.aimAtTheSystemSettingsIfWeCan queries for
    handlers of ACTION_INPUT_METHOD_SETTINGS so it can narrow an implicit intent onto the real
    settings app rather than leaving the user a chooser at the exact moment they have been told to
    go and enable a keyboard - which is the moment a convincing clone is worth the most to an
    attacker. Filtered, that query returns nothing, the method finds no system handler, and it
    falls back to the implicit intent every time. Its own comment calls that fallback "not a fix";
    without this element the fallback was the only path, on every device from Android 11 onward.

    Declared as the exact intent rather than QUERY_ALL_PACKAGES, which is what that permission is
    for and which stores treat as a policy matter. This makes visible the packages that answer this
    one action and nothing else.

    Nothing in this app enumerates installed packages for any other reason, and
    OnlyTheSettingsIntentIsQueriedTest fails if a second query appears without a matching entry.
  -->
  <queries>
    <intent>
      <action android:name="android.settings.INPUT_METHOD_SETTINGS" />
    </intent>
  </queries>

  <application
    android:allowBackup="false"
    android:dataExtractionRules="@xml/data_extraction_rules"
    android:icon="@mipmap/ic_launcher_round"
    android:label="@string/english_ime_name"
    android:supportsRtl="true"
    android:windowSoftInputMode="adjustResize">

    <service
      android:name="com.amnesica.kryptey.inputmethod.latin.LatinIME"
      android:directBootAware="false"
      android:exported="false"
      android:label="@string/english_ime_name"
      android:permission="android.permission.BIND_INPUT_METHOD"
      android:windowSoftInputMode="adjustResize">
      <intent-filter>
        <action android:name="android.view.InputMethod" />
      </intent-filter>

      <meta-data
        android:name="android.view.im"
        android:resource="@xml/method" />
    </service>

    <activity
      android:name="com.amnesica.kryptey.inputmethod.latin.settings.SettingsActivity"
      android:exported="true"
      android:label="@string/english_ime_name"
      android:launchMode="singleTask"
      android:noHistory="true"
      android:theme="@style/platformSettingsTheme"
      android:windowSoftInputMode="adjustResize">
      <intent-filter>
        <action android:name="android.intent.action.MAIN" />

        <category android:name="android.intent.category.LAUNCHER" />
      </intent-filter>
    </activity>

    <receiver
      android:name="com.amnesica.kryptey.inputmethod.latin.SystemBroadcastReceiver"
      android:exported="false">
      <intent-filter>
        <action android:name="android.intent.action.LOCALE_CHANGED" />
      </intent-filter>
    </receiver>
  </application>

</manifest>